[project]
name = "ansible-automation"
version = "0.1.0"
description = "Ansible automation for back-channel"
requires-python = ">=3.12"
dependencies = [
    "ansible>=9.0.0",
    "ansible-core>=2.16.0"
]


